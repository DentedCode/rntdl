{
	"name": "api-not-to-do-app",
	"version": "1.0.0",
	"description": "this is the backend api for the not to do list",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"start": "node api-not-to-do-app/app.js",
		"dev": "nodemon api-not-to-do-app/app.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix react-not-to-do-list && npm run build --prefix react-not-to-do-list"
	},
	"author": "Prem Aharya",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.19.0",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"mongoose": "^5.12.2"
	}
}
